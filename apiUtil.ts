export const constructTranslateApiUrl = (
  sourceLang: string,
  targetLang: string,
): string => {
  /* Replace constructedAPIURL with the API URL that's going to be used for translation,
     along with sourceLang and targetLang if appropriate as part of the URL */
  const constructedAPIURL = ``;
  return constructedAPIURL;
};
